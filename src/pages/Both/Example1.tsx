import Button from '@material-ui/core/Button';

/**
 * No need to console.log(Button) or visit the
 * docs (https://material-ui.com/components/buttons/)
 * when consuming a third-party component.
 * Inline documentation brings everything into your editor 🎉.
 */
const Example1 = () => (
  <div>
    <div>Consuming third-part libraries</div>

    <Button>My button</Button>
  </div>
);

export default Example1;
