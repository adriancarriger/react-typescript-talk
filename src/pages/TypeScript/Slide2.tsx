import Slide from 'components/Slide';

const Slide2 = () => (
  <Slide
    title="TypeScript Features"
    list={[
      'Ability to describe the shape of an object',
      'Help catch problems earlier',
      'Inline and always up-to-date documentation',
      'Nothing changes at runtime',
    ]}
  ></Slide>
);

export default Slide2;
